// Package arch holds the architecture itself as a test: the dependency rules
// the backend's packaging rests on, asserted against what the Go tool chain
// reports rather than against what the tree looks like (ADR 0009).
package arch
